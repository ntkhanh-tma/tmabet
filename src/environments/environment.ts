export const environment = {
  production: false,
  // During development point to the same deployed Apps Script URL
  // (Apps Script has no local emulator — deploy once and use it everywhere)
  appsScriptUrl: '',
};
